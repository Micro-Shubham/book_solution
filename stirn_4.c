#include <stdio.h>
#include <string.h> //this preprocessor is use to access the some library of string.
int main()
{
    char str[] = "Let's work hard for our future.";
    printf("%s", str); //%s operator is use to print the string.Just write the name of string to print it.
    // Note:%s is only accessable if I use string.h preprocessor.

    return 0;
}