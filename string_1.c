// A string is a sequence of characters terminated with a null character \0.
//  Note:The null character assign in the last by itself.
#include <stdio.h>
int main()
{
    char a[5] = {'a', 'c', 'y', 'q', 'p'};
    printf("%c", a[5]);
    return 0;
}