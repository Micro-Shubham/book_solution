#include <stdio.h>
#include <string.h>
int main()
{
    char str[40];
    gets(str); // taking input for string and whole sentence will be considered.

    puts(str); // print the input being entered by the user.
    return 0;
}